{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "LiteralType",
  "description": "",
  "type": "object",
  "properties": {
    "value": {
      "anyOf": [
        {
          "enum": [
            false
          ]
        },
        {
          "enum": [
            0
          ]
        },
        {
          "enum": [
            100
          ]
        },
        {
          "enum": [
            "foo"
          ]
        }
      ]
    }
  },
  "additionalProperties": false,
  "required": [
    "value"
  ]
}